package com.example.testapp.ui.theme

import androidx.compose.ui.graphics.Color

val PrimaryColor = Color(0xFF34A853);            // Green primary color
val DisabledPrimaryColor = Color(0xFFA1E6B3)     // Light muted green
val LightPrimaryColor = Color(0xFFE8FAE9);       // Very light green for backgrounds

val TextColor1 = Color(0xFFA9A8A8);              // Kept the same light gray
val TextColor2 = Color(0xFF3C3C3C);              // Kept the same dark gray
val TextColor3 = Color(0xFFCFCFCF);              // Kept the same very light gray

val BorderPrimaryColor = Color(0xFF34A853);      // Green primary for borders
val BorderSecondaryColor = Color(0xFF4CAF50);    // Slightly different green for secondary borders
val BorderDarkColor = Color(0xFFA1A1A1);         // Kept the same dark border
val TextFieldColor = Color(0xFFF8F7F7);          // Kept the same light background for text fields

val LightGrayColor = Color(0xFFF3F3F3);          // Kept the same light gray
val LightBgColor = Color(0xFFF7F7F7);            // Kept the same light background
val YellowColor = Color(0xFF8BC34A);             // Changed to light green instead of yellow

val GradientColor1 = Color(0xFF34A853);          // Vibrant green for gradient start
val GradientColor2 = Color(0xFFFFEB3B);        // Slightly different green for gradient end

val RedColor = Color(0xFFed3b3b)                 // Kept red for error states or warnings